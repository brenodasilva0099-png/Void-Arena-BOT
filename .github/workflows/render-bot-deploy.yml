name: Deploy BOT to Render

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Trigger Render BOT Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render BOT deploy hook
        run: curl -fsS -X POST "${{ secrets.RENDER_BOT_DEPLOY_HOOK }}"
