// Patch neutralizado temporariamente para impedir SyntaxError no boot.
// A listagem das calls continua funcionando pelo fallback do SITE via /internal/discord/channels.
console.log('Patch ignorado: admin de calls será refeito em versão estável.');
